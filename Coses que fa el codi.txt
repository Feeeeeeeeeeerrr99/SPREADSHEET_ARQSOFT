No pot fer SUMA(3+4;5)


no pots fer coses com SUMA(A1:B2)+SUMA(A1;A2;4) perque el codi detecta com a argument2 (A1:B2)(A1;A2;4) tot junt, el matcher no funciona be.Sha de fer qeu es separi per formules: "A1:B2)(A1;A2;4"



mirar porque se ha camibar FOrmulaCell creation para entrar currentCell pero queremos el string no la cell
Tambien mirar que no se ha hecho las dependencuas cada vesz que hacemos un Expresion parser deberia cambiar el motodo de imputs

Dependencies funcionen pero expressions aixi =3*4+1*(8+9)+A1 no per el A1 que depen de altres?

Aixo tampoc funciona =SUMA(2;3;4)+SUMA(A1:A3) ni amb dependencies

Dependencies funcionen amb formules basiques =SUMA(MIN(A1;A2);4;5) aixo funciona i csi camvies A1 tambe funciona

Veure darreglar lo de les vistes i no vistes de ExpressionParser


